#include <iostream>
using namespace std;

int main(){
    /*

    /// IF ESLE
    int age=6;
    
    if(age>18){
        cout<<"You can Vote"<<endl;
    }else{
        cout<<"You cann't Vote"<<endl;
    }
    
    

    ///TERNARY
    int num;
    cout<<"Enter a number : ";
    cin>>num;
    string ans = (num > 0 ) ? "Positive" : ((num==0) ? "Zero" :"Negative") 
    cout<<ans;

    */

//Write a cpp programm to find largest number among three
    // int a,b,c;
    // cout<<"Enter First number :";
    // cin>>a;
    // cout<<"Enter Second number :";
    // cin>>b;
    // cout<<"Enter Third number :";
    // cin>>c;

    // int lar =(a > b)?( ( a > c) ? a : b ) : ((b > c ) ? b : c);
    // cout<<"The largest Numbers is: "<<lar<<endl;

}